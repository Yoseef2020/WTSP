- الاسم: إجراءات Infracos
  # يمكنك تثبيت التثبيت الدقيق أو الإصدار.
  # الاستخدامات: infracost / Actions @ 0d27b734c72a1abb0aba3e2c3f54e4982cbd2591
  يستخدم: infracost/actions@v2.1.0
  مع:
    # مفتاح Infracost API الخاص بك. يمكن استرجاعها عن طريق تشغيل `infracost config get api_key`. نوصي باستخدام مفتاح API نفسه في جميع البيئات. إذا لم يكن لديك واحد ، فقم بتنزيل Infracost (https://www.infracost.io/docs/#quick-start) وقم بتشغيل `infracost auth login` للحصول على مفتاح API مجاني.
    مفتاح API:
    # المسار الذي سيتم تمريره إلى "الانهيار الأساسي". قد يكون هذا مسارًا إلى JSON لخطة Terraform أو مشروع Terraform.
    طريق:
    # السلوك الذي يجب استخدامه عند نشر تعليقات تقدير التكلفة. يجب أن يكون أحد "التحديثات" | "حذف وجديد" | "إخفاء وجديد" | 'جديد'.
    السلوك: # اختياري ، الافتراضي هو 
